import { ReactNode } from "react";

export const metadata = {
  title: "Admin | Preorder",
  description: "Admin dashboard for Preorder Makanan",
};

export default function AdminLayout({ children }: { children: ReactNode }) {
  return children;
}
